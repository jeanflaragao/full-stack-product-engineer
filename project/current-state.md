# Current State

**Status:** Placeholder — not yet populated.

This is the most important operational memory in the repository: it must describe what is currently true, right now — not what was once planned, and not what the roadmap aspires to. Any AI agent should be able to read this file and know where things actually stand before doing anything else.

It has not yet been populated with a confirmed snapshot of the project's current state. Populating it requires inspecting `betting-platform` directly (its `README.md`, `ROADMAP.md`, `SESSION.md`, and `docs/adr/`) rather than guessing.

TODO: populate with a confirmed current-state snapshot.
